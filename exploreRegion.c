#include "main_header.h"

void exploreRegion(player * hero)
{
	int outcome;

	/*outcome = random();
	printw("outcome= %d", outcome);

	if(outcome > 0 && outcome < 30)
		findItem(hero);
	
	if(outcome > 30 && outcome < 60)
		findItem(hero);		

	if(outcome > 60 && outcome < 90)
		return;

	if(outcome > 90)
		return;*/
}


